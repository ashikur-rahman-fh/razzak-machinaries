# Rate limit zones (http context). Uses $remote_addr after real-ip module (Cloudflare).
limit_req_zone $remote_addr zone=general:10m rate=10r/s;
limit_req_zone $remote_addr zone=api:10m rate=30r/s;
limit_req_zone $remote_addr zone=login:10m rate=5r/m;
limit_req_status 429;
