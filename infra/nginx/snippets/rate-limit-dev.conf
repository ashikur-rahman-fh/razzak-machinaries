# Looser rate limits for local development (http context). Direct client IPs (no Cloudflare).
limit_req_zone $remote_addr zone=general:10m rate=50r/s;
limit_req_zone $remote_addr zone=api:10m rate=100r/s;
limit_req_zone $remote_addr zone=login:10m rate=30r/m;
limit_req_status 429;
