PROJECT_NAME=razzak-machinaries
# Docker Compose project name (container prefix). Keep stable after first deploy — changing it creates new volumes.
COMPOSE_PROJECT_NAME=razzak-machinaries-prod
# Production (config.settings.prod): SECRET_KEY and POSTGRES_PASSWORD must be real secrets.
# Placeholders starting with "change-me" are rejected — Gunicorn workers will not start.
ENVIRONMENT=prod
DEBUG=false
SECRET_KEY=change-me-generate-strong-secret
DJANGO_SETTINGS_MODULE=config.settings.prod
# Keep `backend` and/or `127.0.0.1` in ALLOWED_HOSTS so deploy health checks from inside the container are not HTTP 400 (DisallowedHost).
ALLOWED_HOSTS=razzak-machinaries.com,www.razzak-machinaries.com,admin.razzak-machinaries.com,api.razzak-machinaries.com,backend,127.0.0.1
CSRF_TRUSTED_ORIGINS=https://razzak-machinaries.com,https://www.razzak-machinaries.com,https://admin.razzak-machinaries.com,https://api.razzak-machinaries.com
CORS_ALLOWED_ORIGINS=https://razzak-machinaries.com,https://www.razzak-machinaries.com,https://admin.razzak-machinaries.com

POSTGRES_DB=razzak_machinaries_prod
POSTGRES_USER=razzak_machinaries
POSTGRES_PASSWORD=change-me-strong-db-password
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

REDIS_URL=redis://redis:6379/0

BACKEND_URL=https://api.razzak-machinaries.com
# Browser-facing API URL (baked into Next at image build). Must be https:// and match your
# public API host — same origin as API_HOST with https, never http://backend:8000 or localhost.
NEXT_PUBLIC_BACKEND_MAIN_API_URL=https://api.razzak-machinaries.com
NEXT_PUBLIC_BASE_PATH=
# Theme mode for both frontend apps: light | dark | system.
# Empty, missing, or invalid values safely fall back to system behavior.
NEXT_PUBLIC_THEME_MODE=system

# Nginx vhosts (rendered into infra/nginx/prod/conf.d/default.conf via make prod-nginx-config)
MAIN_FRONTEND_HOST=razzak-machinaries.com
MAIN_FRONTEND_WWW_HOST=www.razzak-machinaries.com
ADMIN_FRONTEND_HOST=admin.razzak-machinaries.com
API_HOST=api.razzak-machinaries.com

# Registry deploy on the VM (make prod-deploy IMAGE_TAG=staging-260516-1)
# Copy the same values as GitHub Environment staging_env / release_env. Never commit real tokens.
DOCKER_REPO=yourorg/razzak-machinaries-staging
BRANCH_SLUG=staging
DOCKER_USERNAME=
DOCKER_TOKEN=
BACKUP_DIR=/var/backups/razzak-machinaries
# Public API host for health checks (defaults to API_HOST if unset)
APP_DOMAIN=api.razzak-machinaries.com
# HEALTH_CHECK_HTTP_HOST=api.razzak-machinaries.com
# HEALTH_CHECK_PATH=/api/health/

# Security (optional overrides)
# CLIENT_MAX_BODY_SIZE=25m
# DRF_THROTTLE_ANON=100/hour
# DRF_THROTTLE_API=200/hour
# AXES_FAILURE_LIMIT=5
# AXES_COOLOFF_MINUTES=30
# DATA_UPLOAD_MAX_BYTES=2621440
# CSP_REPORT_ONLY=false
# CSP_REPORT_URI=
# GIT_REF=master
# SKIP_GIT_SYNC=1

WAIT_FOR_DEBUGGER=false

# Admin superusers (JSON array). Idempotent sync: make prod-sync-superusers
# ADMIN_SUPERUSERS='[{"username":"admin","email":"admin@example.com","password":"change-me-in-env","first_name":"Admin","last_name":"User"}]'
# ADMIN_SUPERUSER_UPDATE_PASSWORD=false
