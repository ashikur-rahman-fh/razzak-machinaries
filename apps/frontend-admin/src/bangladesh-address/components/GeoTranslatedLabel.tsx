'use client';

import { TranslatedText, type TranslatedTextProps } from '@razzak-machinaries/shared/ui';

export function GeoTranslatedLabel(props: TranslatedTextProps) {
  return <TranslatedText {...props} />;
}
